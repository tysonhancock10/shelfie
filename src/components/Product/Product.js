import React from 'react'

const Product = (props) => {

    return(
        <div> Product
            {/* <h1>{props.inventory.name}</h1>
            <h1>{props.inventory.price}</h1>
            <img>{props.inventory.image}</img> */}
        </div>
    )
}
export default Product